import {connect} from "react-redux"
import Node from "./Node"

let mapStateToProps = () => ({

})

let mapDispatchToProps = () => ({

})

let NodeContainer = connect(mapStateToProps, mapDispatchToProps)(Node)

export default NodeContainer;